# config for minikube
mounted_volumes:
  - name: shared-folder
    sub_path: '{{project_name}}'
    spec:
      hostPath:
        path: /host


# config for google cloud
# code_pod:
#   args: -l role=nfs-server
#   path: /exports/{{project_name}}
