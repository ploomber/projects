# uncomment if you run this in Argo and want to save artifacts to cloud storage
# args: --env--run_id {{workflow.uid}}
cache_env: False

# config for minikube
mounted_volumes:
  - name: shared-folder
    sub_path: '{{project_name}}'
    spec:
      hostPath:
        path: /host


# config for google cloud
# code_pod:
#   args: -l role=nfs-server
#   path: /exports/{{project_name}}
